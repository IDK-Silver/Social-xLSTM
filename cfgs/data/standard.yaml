# Development Dataset Configuration
# Small dataset for quick iteration and testing
# Decoupled from model architecture and training parameters

data:
  # dataset file path
  path: "blob/dataset/processed/pems_bay.h5"

  # data splitting ratios
  split:
    train: 0.7
    val:   0.3
    test:  0.0
  
  # dataLoader configuration  
  loader:
    batch_size: 16
    num_workers: 8
    pin_memory: true
    shuffle: true
  
  # data preprocessing - common settings
  normalize: true
  # sliding window stride
  stride: 10                         
